#include "holberton.h"
#include <stdio.h>
/**
 * main - first declaration
 * @argc: number of arguments
 * @argv: array that contains the arguments
 * Return: 0
 */
int main(int argc __attribute__((unused)),
	char *argv[] __attribute__((unused)))
{
	printf("%s\n", *argv);
	return  (0);
}
